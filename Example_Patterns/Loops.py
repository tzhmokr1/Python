# Iterate through dictionary
for key,value in device_dict.items(): 
   ip_addr = key
   device_info = value

   for key in device_dict: 
      ip_addr = key
      device_info = device_dict[ip_addr]

# Rnage in for loop 
   for num in range(start, [len]stop, step):






# While Loop
   file = open('devices','r')   
   line = file.readline()
   while line:
      # do something
      line = file.readline()


while True:
      name = raw_input('Enter device name or <cr>:')
      print 'Name:',name if name else exit()


